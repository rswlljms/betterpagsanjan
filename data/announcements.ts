import type { Announcement } from "@/types/civic";

/**
 * No announcements are published yet. BetterPagsanjan only lists
 * announcements, advisories, and notices that can be traced to an
 * official source (AGENTS.md §14). Verified items will be added here
 * with their source and publication date.
 */
export const announcements: Announcement[] = [];
